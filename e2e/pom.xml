<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>de.dfki.asr.ajan</groupId>
  <artifactId>ajan-e2e</artifactId>
  <version>0.1</version>
  <packaging>jar</packaging>
  <name>AJAN E2E Smoke Suite</name>
  <!-- Bewusst KEIN Modul des ajan-parent-Reaktors: die Suite muss alle
       Migrationsetappen unveraendert ueberleben und testet nur von aussen. -->

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Default leer: erlaubt -Dajan.e2e.triplestoreUrl=... auf der mvn-Kommandozeile,
         ohne dass ${ajan.e2e.triplestoreUrl} unaufgeloest bleibt, wenn der Hook nicht
         gesetzt wird. -->
    <ajan.e2e.triplestoreUrl></ajan.e2e.triplestoreUrl>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
    <!-- BEWUSST auf RDF4J 3.6.3 belassen, obwohl das Projekt seit Etappe 3
         auf 5.3.1 steht (Auftraggeber-Entscheidung 2026-08-11): die Suite
         testet ausschliesslich von aussen ueber HTTP und soll alle
         Migrationsetappen unveraendert ueberleben. Ein vom Produktcode
         entkoppelter Rio-Parser ist dafuer das robustere Werkzeug; er
         parst die Antworten des Service, nicht dessen Interna.
         Test-scope only: die E2E-Suite hat keine Produktionsabhaengigkeit
         auf RDF4J. -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-turtle</artifactId>
      <version>3.6.3</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-model</artifactId>
      <version>3.6.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.2.5</version>
        <configuration>
          <systemPropertyVariables>
            <ajan.root>${project.basedir}/..</ajan.root>
            <!-- Explizite Deklaration des Hook-Properties: macht den Vertrag
                 "-Dajan.e2e.triplestoreUrl auf der mvn-Kommandozeile erreicht die
                 geforkte Test-JVM" sichtbar, statt ihn von der internen
                 Property-Propagation von Surefire/Failsafe abhaengen zu lassen.
                 ACHTUNG, keine allgemeine Regel: Maven-User-Properties (-D) kommen
                 auch OHNE Eintrag hier im Fork an. Die urspruengliche Spike-A-Diagnose
                 ("Failsafe leitet bei forkCount=1 nicht weiter, der Hook war
                 wirkungslos") wurde im Whole-Branch-Review widerlegt - mit dem
                 byte-identischen Pre-Fix-POM unter Maven 3.9.16 / failsafe 3.2.5 /
                 JDK 11 kam das Property sehr wohl in der Test-JVM an. Diese Zeile
                 bleibt, weil sie harmlos und robust gegen kuenftige
                 Surefire-Versionen ist, nicht weil sie eine Luecke schliesst. -->
            <ajan.e2e.triplestoreUrl>${ajan.e2e.triplestoreUrl}</ajan.e2e.triplestoreUrl>
          </systemPropertyVariables>
          <!-- ITs starten echte Prozesse auf festen Ports: strikt sequenziell -->
          <forkCount>1</forkCount>
          <reuseForks>true</reuseForks>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
